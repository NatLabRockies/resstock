\begin{itemize}
\item Where samples are less than 10, the data is aggregated in the following order until there are no rows with less than 10 samples:
\item 1. The Federal Poverty Level dependency is aggregated every 100\%
\item 2. The Federal Poverty Level dependency is aggregated every 200\%
\item 3. The Geometry Building Type RECS dependency is aggregated into SF, MF, and MH bins.
\item 4. The Cooling Unavailable Days dependency is aggregated into Days, Weeks, Month, and All Year bins.
\item 5. The Cooling Unavailable Days dependency is removed.
\end{itemize}
