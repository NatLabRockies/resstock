\begin{itemize}
\item Due to low sample sizes, fallback rules applied with lumping of:
  \begin{itemize}
  \item Federal Poverty Level lumped every 100\%
  \item Federal Poverty Level lumped every 200\%
  \item Building type is consolidated into three bins 1) single-family, 2) multi-family, and 3) mobile home bins
  \item Building type is consolidated into two bins 1) single-family and 2) multi-family and mobile home bins
  \end{itemize}
\item PUMA level battery electric vehicle saturation is calculated using a weighted average of the County level Experian data and ResStock unit counts for each County and PUMA.
\item The RECS 2020 saturations for each segment in a given PUMA are scaled to match the Experian PUMA weighted averaged battery electric vehicle saturation using the segment unit counts from PUMS 2019-5yrs survey.
\end{itemize}
