Files in the `assumptions`, `dependencies`, `description`, `options`, `properties`, and `sources` subfolders are automatically generated by `create_characteristics_tex.rb`.
Files in the `characteristics` parent folder are not; they are referenced by the reference guide tex files (e.g., `3a_ResStockInputs.tex`). Their contents reference files in folders listed above, and can contain manually added text.
